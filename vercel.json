{
  "version": 2,
  "builds": [
    { "src": "frontend/package.json", "use": "@vercel/static-build", "config": { "distDir": "build" } }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "https://helth-2.onrender.com/
/api/$1" },
    { "src": "/(.*)", "dest": "/frontend/build/$1" }
  ]
}

